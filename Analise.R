# Criando um vetor x

x <- c(2,4,6,8,10)

# Calculando a média dos valores de x 

mean(x = x, na.rm = T)

# Criando um vetor y
y <- c(1,3,5,7,9)

# Calculando a correlação entre os dois vetores
cor(x = x, y = y)
